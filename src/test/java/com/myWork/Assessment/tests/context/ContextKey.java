package com.myWork.Assessment.tests.context;
/**
 * Enum representing context keys used to store and retrieve values
 * in the scenario context during test execution.
 * <p>
 * These keys provide type-safe access to commonly shared test data.
 */
public enum ContextKey {
    USERNAME,
    EMAIL,
    PASSWORD
}
